{
  "name": "ts-sqlite",
  "version": "1.0.0",
  "description": "sqlite wrapper written in TypeScript",
  "author": "hayouung",
  "license": "ISC",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Hayouung/ts-sqlite.git"
  },
  "scripts": {
    "lint": "tslint --fix -c tslint.json src/**/*.ts",
    "build": "tsc",
    "clean-build": "rmdir /s /q dist && tslint --fix -c tslint.json src/**/*.ts && tsc",
    "prepare": "npm run build",
    "test": "jest"
  },
  "dependencies": {
    "sqlite3": "^4.0.6"
  },
  "devDependencies": {
    "@types/jest": "^24.0.11",
    "@types/sqlite3": "^3.1.5",
    "jest": "^24.7.1",
    "ts-jest": "^24.0.2",
    "tslint": "^5.16.0",
    "typescript": "^3.4.5"
  }
}
